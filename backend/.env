# Django
SECRET_KEY=change-me-in-prod
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (matches docker-compose defaults)
POSTGRES_DB=erpdb
POSTGRES_USER=erpuser
POSTGRES_PASSWORD=erppassword

# Connection target for Django
DB_HOST=127.0.0.1
DB_PORT=5432
